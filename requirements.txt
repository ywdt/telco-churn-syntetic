# Основні залежності для генерації даних
pandas>=2.0.0
numpy>=1.21.0
faker>=25.0.0            # для створення реалістичних даних (якщо використовуєте)

# Для роботи з yaml-конфігурацією
pyyaml>=6.0

# Для notebook / дослідження даних (локально / демо)
jupyterlab>=4.0
matplotlib>=3.7
seaborn>=0.12               # необов’язково, але дуже зручно для гарних графіків

# Дуже рекомендується для подальшого розвитку MLOps
pyyaml                      # вже є вище, але для нагадування
tqdm                        # прогрес-бар при генерації великих датасетів

# ──────────────────────────────────────────────────────────────
# Додаткові залежності (розкоментуйте, коли знадобляться)
# ──────────────────────────────────────────────────────────────

# Для linting та форматування коду (дуже рекомендую)
# ruff>=0.5.0
# black>=24.0

# Для ML та оцінки дрейфу (майбутні модулі)
# scikit-learn>=1.3
# evidently>=0.4.0           # дуже зручний інструмент для оцінки data & concept drift
# lightgbm>=4.0              # або xgboost, catboost — на вибір

# Для API та веб-додатків (коли будете робити чат-агента)
# fastapi>=0.100
# uvicorn>=0.22
# python-multipart             # для fastapi